{
  "scripts": {
    "coverage": "hardhat coverage",
    "deploy": "hardhat run scripts/deploy.ts --network optimism",
    "test": "hardhat test"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/bun": "^1.2.12",
    "dotenv": "^16.5.0",
    "hardhat": "^2.23.0"
  }
}